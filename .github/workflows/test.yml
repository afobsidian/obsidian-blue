name: Test

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: bash tests/validate-recipe.sh
      - run: bash tests/test-omarchy-browser-launcher.sh
      - run: bash tests/test-quattro-migration.sh
