#!/usr/bin/env bash

set -euo pipefail
selection="$(flatpak list --user --app --columns=application,name | fzf --multi)"
[[ -n "$selection" ]] || exit 0
cut -f1 <<<"$selection" | xargs -r flatpak uninstall --user --noninteractive -y
